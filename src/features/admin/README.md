# Admin Feature

System admin manages users and configuration only. It does not grant business approval authority by default.

Phase 3 tests assert that `system_admin` cannot perform business approval writes.
