# Migrations Legacy

`pre-canonical-baseline/` contains the 24 pre-baseline local migrations, archived verbatim as
historical reference when CX-0194 Gate A adopted the canonical baseline.

These files must NOT be used for new database rebuilds. The active canonical chain is
`supabase/migrations/` starting at `20260807120000_pg_os_canonical_baseline.sql`.

See `pre-canonical-baseline/ARCHIVE.md` for the original/new path mapping and content hashes.
