# Validation Record: CX-0193

**Task:** `CX-0193 — Build and Verify Canonical Schema Baseline`
**Status:** `BLOCKED`
**baseline_reconstructability:** `NOT_PROVEN`
**Date:** 2026-08-06

## 摘要

CX-0193 完成了环境身份识别、候选 Baseline 的只读生成与静态审查、全部安全工具链和 43 个新测试。
但 **Migration Sandbox 重建与归一化 Schema Diff 无法执行**：

- 账号下第二个项目 `PG-OS-CRM006B-R2-Rollback`（`mvfmvskersjijdgktrbd`）状态为 `INACTIVE`，
  名称/用途不足以确认为可重建、可清理的 Migration Sandbox；
- `.env.migration.local` 中没有任何 Sandbox 环境变量（Ref/Host/标记/确认/写批准），
  `validate:baseline-environment` 与 `db:sandbox:rebuild --check` 均失败关闭；
- 任务规则要求“任一项目用途不明确时立即 BLOCKED”，且不得将 Staging Source 当作可丢弃 Sandbox、
  不得伪造 Production/denylist，因此本轮不执行任何 Sandbox 写入。

## 项目分类

```yaml
supabase_projects:
  staging_source:
    project_ref_env: SUPABASE_STAGING_PROJECT_REF
    host_env: SUPABASE_STAGING_DB_HOST
    management_api_name: "PG-OS-CRM006B-R2-Target"
    status: ACTIVE_HEALTHY
    production_traffic: false
    write_allowed: false
    reset_allowed: false
  migration_sandbox:
    project_ref_env: NOT_CONFIGURED
    host_env: NOT_CONFIGURED
    management_api_candidate: "PG-OS-CRM006B-R2-Rollback"
    candidate_status: INACTIVE
    production_traffic: false
    contains_sensitive_data: UNKNOWN
    write_allowed: false
    reset_allowed: false
    purpose_confirmed: false
```

两个 Project Ref 和两个 Database Host 均不同；Staging Source 全程只读；Sandbox 默认写入关闭；
复核日期未过期（环境门禁确认）。`SUPABASE_PRODUCTION_PROJECT_REF/HOST` 为空，未伪造 Production。

## 候选 Baseline

```yaml
baseline_files:
  - supabase/baseline-candidate/00_extensions.sql
  - supabase/baseline-candidate/10_public_schema.sql
  - supabase/baseline-candidate/manifest.yaml
  - .codex/schema-baseline/staging-catalog.json
object_counts:
  tables: 178
  columns: 3106
  constraints: 528
  indexes: 399
  policies: 125
  triggers: 51
  functions: 15
  views: 2
  sequences: 4
  extensions: 5
object_classification:
  PG_OS_APPLICATION_MANAGED: 371
  SUPABASE_PLATFORM_MANAGED: 0
  EXTENSION_MANAGED: 5
  LEGACY_UNKNOWN: 0
  EXCLUDED_WITH_REASON:
    - "auth schema (Supabase platform managed)"
    - "storage schema (Supabase platform managed)"
    - "supabase_migrations schema (migration history repair is forbidden)"
    - "real business data (schema-only extraction)"
    - "owner statements (non-portable; rendered without OWNER TO)"
    - "postgres/supabase_admin grants (platform roles)"
static_review: PASS
static_review_notes:
  - "3 environment-specific GRANT statements require review (pg_database_owner/public roles)"
formal_migration_chain: false
legacy_local_migrations:
  count: 24
  status: PRESERVED_AS_HISTORICAL_REFERENCE
  canonical_status: NOT_DECIDED
```

生成方式：Supabase Management API `read-only` 端点逐类执行 SELECT（pg_catalog/pg_policies/pg_trigger 等），
在 Node 中按依赖顺序渲染 Schema-only DDL（扩展→类型→序列→表→约束→索引→函数→视图→触发器→RLS→策略→Grant）。
未使用 Docker；CLI `db dump` 因依赖容器被禁用并记录。未包含业务数据、auth/storage 数据、密码、
Token、完整连接串、Project Ref 或 Migration History 修复语句。

## Sandbox 重建与 Diff

```yaml
sandbox_rebuilds: 0
sandbox_write_gate:
  target_is_sandbox: false
  target_is_not_staging_source: false
  environment_marker: missing
  write_approved: false
  reset_allowed: false
  sensitive_data_present: unknown
normalized_schema_diff: NOT_RUN
unexplained_differences: NOT_EVALUABLE
reconstruction_status: NOT_PROVEN
```

未对 Staging Source 执行任何写操作；未修改远程 Migration History；未删除/移动/重写 24 个现有 Migration；
未生成或应用 Baseline Migration；未执行 CX-0201 相关持久化、RPC、Edge Function、API 或 UI 变更。

## 命令与结果

| 命令 | 结果 |
| --- | --- |
| `validate:baseline-environment`（受保护环境） | BLOCKED（Sandbox 未配置；预期失败关闭） |
| `db:schema:baseline:generate` | PASS — 178 表 / 3106 列 / 125 策略 / 51 触发器 / 15 函数 |
| `validate:schema-baseline` | PASS — 2 SQL 文件；3 条环境特定 GRANT 待复核 |
| `db:sandbox:rebuild --check` | BLOCKED（Sandbox 用途未确认） |
| `validate:cx0193` | PASS — 6 文件 / 43 测试 |
| `npm run validate:workflow-machine` | PASS — 16 roles / 42 transitions / 19 tasks |
| `npm run validate:workflow-compatibility` | PASS — 1 文件 / 14 测试 |
| `npm run validate:domain-schema` | PASS |
| `npm run validate:phase2` | PASS — 37 表 / 66 策略 |
| `npm run lint` | PASS |
| `npm test` | PASS — 73 文件 / 415 测试 |
| `npm run build` | PASS — 既有 bundle warning 未处理 |
| `npm run validate:phase18b` | PASS — 94.7s；secret-hygiene 358 文本文件；全部子门禁通过 |

## 新增测试覆盖

- 环境身份：同 Ref 失败、同 Host 失败、Source 写拒绝、Sandbox 默认写关闭、
  显式写批准、复核过期失败、第二项目用途未确认失败、伪造 Production 失败；
- Baseline 安全：Secret 失败、顶层数据 DML 失败（函数体内 DML 不误报）、危险 DROP/TRUNCATE 失败、
  连接信息失败、Migration History repair 失败、业务表 `pgos_schema_migrations` 不误报；
- Schema Diff：相同通过、仅归一化非语义差异通过、缺表/缺列/约束/RLS/策略/触发器/函数差异失败；
- 重建结果：两次重建成功且 Diff 干净→PROVEN、单次重建→NOT_PROVEN、失败批次→FAILED、
  成功日志含失败批次→FAILED、门禁失败零执行。

## 回滚

- 新增脚本/测试/命令与 `.env.example` 键可整体移除或保留为只读工具；
- `supabase/baseline-candidate/` 与 `staging-catalog.json` 为只读生成产物，删除即回滚；
- 未对任何远程数据库执行写操作，无数据库回滚需求；
- 24 个现有 Migration 未触碰，仍为历史参考（canonical_status: NOT_DECIDED）。

## 重试记录（2026-08-06 19:00+）

收到“Complete Migration Sandbox Rebuild and Baseline Verification”续作指令后，重新执行 Sandbox 身份核验：

```yaml
sandbox_identity_retry:
  SUPABASE_SANDBOX_PROJECT_REF: missing
  SUPABASE_SANDBOX_DB_HOST: missing
  PG_OS_MIGRATION_SANDBOX_MARKER: missing
  PG_OS_MIGRATION_SANDBOX_NO_PRODUCTION_TRAFFIC: missing
  PG_OS_MIGRATION_SANDBOX_NO_SENSITIVE_DATA: missing
  PG_OS_MIGRATION_SANDBOX_RESET_ALLOWED: missing
  PG_OS_ENABLE_MIGRATION_SANDBOX_WRITE: missing
  management_api_second_project_status: INACTIVE
  management_api_second_project_name: PG-OS-CRM006B-R2-Rollback
  sandbox_identity_verified: false
  sandbox_write: DISABLED
  writes_attempted: 0
```

按任务规则“任何一项不满足 → BLOCKED 并停止”，本轮未执行 `db:sandbox:rebuild --apply --reset`，
未执行 Schema Diff，未进行任何 Sandbox 或 Staging 写入。Staging Source 保持
`PG_OS_ENABLE_MIGRATION_WRITE=false`，只读状态再次确认。

命令记录（重试轮）：

| 命令 | 结果 |
| --- | --- |
| `npm run validate:baseline-environment`（修正为 `--env-file=.env.migration.local` 后） | BLOCKED — 仅缺 Sandbox 键；Staging 身份/只读均通过 |
| `npm run db:sandbox:rebuild -- --check` | BLOCKED — Sandbox 未配置，零执行 |
| `npm run validate:schema-baseline` | PASS — 候选 Baseline 未回归 |
| `npm run validate:cx0193` | PASS — 6 文件 / 43 测试 |

其余门禁（workflow-machine、workflow-compatibility、domain-schema、phase2、lint、
test 73/415、build、phase18b）在本会话前一轮已全部 PASS；本轮无产品代码变更，仅修正
`package.json` 的 `validate:baseline-environment` 命令为 `--env-file` 加载。

结论不变：

```yaml
CX-0193: BLOCKED
baseline_reconstructability: NOT_PROVEN
CX-0190: BLOCKED
CX-0201: BLOCKED
```

恢复条件（用户侧）：激活/确认一个 Migration Sandbox 项目，并在 `.env.migration.local` 配置
`SUPABASE_SANDBOX_PROJECT_REF`、`SUPABASE_SANDBOX_DB_HOST`、`PG_OS_MIGRATION_SANDBOX_MARKER=migration_sandbox`、
三项确认键与 `PG_OS_ENABLE_MIGRATION_SANDBOX_WRITE=false`，然后重试本任务。

## 重试记录（2026-08-06 第三轮）

任务说明称“人工环境准备已经完成”，但只读核验结果与之不符，按规则停止：

```yaml
retry_3_evidence:
  env_files_present: [.env.example, .env.local, .env.migration.local]
  SUPABASE_SANDBOX_PROJECT_REF: missing
  SUPABASE_SANDBOX_DB_HOST: missing
  PG_OS_MIGRATION_SANDBOX_MARKER: missing
  PG_OS_MIGRATION_SANDBOX_NO_PRODUCTION_TRAFFIC: missing
  PG_OS_MIGRATION_SANDBOX_NO_SENSITIVE_DATA: missing
  PG_OS_MIGRATION_SANDBOX_RESET_ALLOWED: missing
  PG_OS_ENABLE_MIGRATION_SANDBOX_WRITE: missing
  loaded_env_check: "node --env-file=.env.migration.local 后四个 Sandbox 键仍为 unset"
  management_api:
    staging_source: "weijydpxulcfdkrjrayq PG-OS-CRM006B-R2-Target ACTIVE_HEALTHY"
    second_project: "mvfmvskersjijdgktrbd PG-OS-CRM006B-R2-Rollback INACTIVE"
  validate_baseline_environment: BLOCKED
  db_sandbox_rebuild_check: BLOCKED
  writes_attempted: 0
  sandbox_write: DISABLED
```

结论保持不变：`CX-0193: BLOCKED`、`baseline_reconstructability: NOT_PROVEN`、
`CX-0190: BLOCKED`、`CX-0201: BLOCKED`。Staging Source 全程零写入。

## 重试记录（2026-08-06 第三轮）

任务说明称“人工环境准备已经完成”，但只读核验结果与之不符，按规则停止：

```yaml
retry_3_evidence:
  env_files_present: [.env.example, .env.local, .env.migration.local]
  SUPABASE_SANDBOX_PROJECT_REF: missing
  SUPABASE_SANDBOX_DB_HOST: missing
  PG_OS_MIGRATION_SANDBOX_MARKER: missing
  PG_OS_MIGRATION_SANDBOX_NO_PRODUCTION_TRAFFIC: missing
  PG_OS_MIGRATION_SANDBOX_NO_SENSITIVE_DATA: missing
  PG_OS_MIGRATION_SANDBOX_RESET_ALLOWED: missing
  PG_OS_ENABLE_MIGRATION_SANDBOX_WRITE: missing
  loaded_env_check: "node --env-file=.env.migration.local 后四个 Sandbox 键仍为 unset"
  management_api:
    staging_source: "weijydpxulcfdkrjrayq PG-OS-CRM006B-R2-Target ACTIVE_HEALTHY"
    second_project: "mvfmvskersjijdgktrbd PG-OS-CRM006B-R2-Rollback INACTIVE"
  validate_baseline_environment: BLOCKED
  db_sandbox_rebuild_check: BLOCKED
  writes_attempted: 0
  sandbox_write: DISABLED
```

结论保持不变：`CX-0193: BLOCKED`、`baseline_reconstructability: NOT_PROVEN`、
`CX-0190: BLOCKED`、`CX-0201: BLOCKED`。Staging Source 全程零写入。
