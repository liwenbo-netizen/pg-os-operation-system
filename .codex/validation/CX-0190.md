# Validation Record: CX-0190

**Task:** `CX-0190 — Prepare Remote Supabase Migration Safety Environment`  
**Status:** `BLOCKED`  
**Date:** 2026-08-02

## Summary

CX-0190 now has a pinned project-local Supabase CLI, CLI configuration, migration-safety environment contract, production-denylist checks, secret-safe environment template, direct regression coverage, and protected local Staging credentials. The repository owner attested that the existing remote project has no production traffic, no sensitive or required data, and may be cleared or rebuilt.

The protected Project identity matches the existing application configuration. CLI authentication and a read-only remote Migration History query pass. The task remains `BLOCKED` because the account exposes exactly one Project and therefore has no real production Project Ref/Host for the mandatory denylist. Writes remain disabled. No remote project was linked, no SQL or migration was applied, and no production system was accessed.

## Readiness Evidence

```yaml
supabase_cli:
  local_node_version: v24.15.0
  ci_node_version: 22
  global_command: false
  repository_local_binary: true
  package_lock_entry: true
  fixed_version: 2.110.0
  approved_execution_method: PROJECT_DEV_DEPENDENCY
  command_prefix: npx supabase
  validation_command: npm run validate:supabase-cli
  validation_result: PASS
  windows_execution: "Official package supabase-go.exe through SUPABASE_CLI_BINARY_OVERRIDE"
repository_configuration:
  supabase_config_toml: true
  migration_environment_scripts: true
  production_denylist_script: true
  schema_diff_scripts: false
  migration_safety_gate: true
database_tools:
  psql: false
  pg_dump: false
docker:
  installed: false
  required_by_plan: false
ci:
  database_job: false
  current_sql_coverage: "npm run validate:phase2 performs static checks only; validate:phase18b does not apply migrations."
migrations:
  directory: supabase/migrations
  count: 24
  naming: YYYYMMDDNNNN_description.sql
  documentation_gap: false
non_production_environment:
  verified: true
  owner: repository_owner
  isolation_proof: "Owner attestation plus protected identity matching, CLI account visibility, and successful read-only database authentication. The account exposes exactly one Project."
  disposable_or_cleanable: true
  production_traffic_absent: true
  sensitive_data_absent: true
  target_allowlist: [protected_local_environment]
  production_denylist: []
required_environment_variables:
  SUPABASE_STAGING_PROJECT_REF: protected_local
  SUPABASE_STAGING_DB_URL: protected_local
  SUPABASE_STAGING_DB_PASSWORD: protected_local
  PG_OS_DATABASE_ENV: staging
  SUPABASE_PRODUCTION_PROJECT_REF: absent
  SUPABASE_PRODUCTION_DB_HOST: absent
  SUPABASE_ACCESS_TOKEN: protected_local
  PG_OS_ENABLE_MIGRATION_WRITE: false
secret_isolation:
  env_example_tracked: true
  env_local_ignored: true
  committed_real_secrets_detected: false
  migration_non_production_marker: true
  note: "Real migration values are stored only in .env.migration.local, confirmed Git-ignored. No real value is included in this record, command output, fixture, or tracked file."
```

## Blocked Commands

The local prerequisite commands exist and fail closed. Protected Staging access is configured, but write-capable remote capabilities cannot be exercised until the production-denylist contract is resolved:

| Capability | Status | Blocking input |
| --- | --- | --- |
| Supabase CLI version check | PASS | `supabase@2.110.0`, project-local `npx` execution |
| Migration environment safety check | BLOCKED | Protected Staging values pass identity/read checks; production Project Ref and Host are absent |
| Production denylist check | BLOCKED | The account exposes exactly one Project, so no real production Project Ref/Host is available |
| Migration Dry Run | BLOCKED | Safety environment and explicit-write gate have not passed |
| Schema Diff | BLOCKED | Remote baseline has not been inspected |
| Migration safety total gate | BLOCKED | Dry run, diff, idempotency, rollback and cleanup capabilities |

## Required Human Approval and Inputs

1. Resolve the approved safety contract for an account with no separate production Project: either provision a real production-denylist target or explicitly approve a first-class no-production-project marker. Placeholder values are not acceptable.
2. Keep `PG_OS_ENABLE_MIGRATION_WRITE=false` until the denylist decision, read-only remote Schema baseline review, and executable rollback path are complete.

## Commands and Results

The repository's non-database-writing gates were rerun after the local toolchain changes. Remote migration commands remain `BLOCKED`, not `PASS`.

| Command | Result |
| --- | --- |
| `npm.cmd install --save-dev --save-exact supabase@2.110.0` | PASS after a long platform-package download; package and lockfile are exact |
| `npm.cmd run validate:supabase-cli` | PASS — project-local CLI reports 2.110.0; Docker not required |
| `npm.cmd exec vitest run scripts/supabaseMigrationSafety.test.mjs scripts/validate-secret-hygiene.test.mjs` | PASS — 2 files, 10 tests |
| `npx supabase projects list --output json` with protected environment | PASS — Token authenticates and the original Project is visible; raw identity values were not logged |
| `npx supabase migration list --db-url <protected> -o json` | PASS — database password and read-only remote Migration History access verified; URL and history values were not logged |
| `npm.cmd run validate:migration-environment` with protected environment | EXPECTED BLOCK — only `SUPABASE_PRODUCTION_PROJECT_REF` and `SUPABASE_PRODUCTION_DB_HOST` are absent |
| `npm.cmd run validate:production-denylist` | EXPECTED BLOCK — no separate production Project exists |
| `npm.cmd run validate:migration-safety` | EXPECTED BLOCK — CLI passes, then the absent production denylist stops the aggregate gate |
| `Get-Command psql` / `Get-Command pg_dump` | BLOCKED — tools absent |
| `npm.cmd run validate:workflow-machine` | PASS — 16 roles, 42 transitions, 19 tasks |
| `npm.cmd run validate:workflow-compatibility` | PASS — 1 file, 14 tests |
| `npm.cmd run validate:domain-schema` | PASS — six Opportunity stages aligned |
| `npm.cmd run validate:phase2` | PASS — 37 tables, 66 policies; static SQL only |
| `npm.cmd run lint` | PASS — `tsc --noEmit` |
| `npm.cmd run build` | PASS WITH EXISTING WARNING — 1,294.31 kB minified JS chunk |
| `npm.cmd test` | PASS — 66 files, 359 tests |
| `npm.cmd run validate:phase18b` | PASS — local-only UAT gate, 66 files / 359 tests |

`validate:phase2` and `validate:phase18b` do not connect to an isolated database or apply migrations. Their success is repository regression evidence only, not migration Dry Run evidence.

## Excluded Work

- No `CX-0201` implementation or five-dimensional Workflow persistence.
- No migration generation, application, backfill, schema diff, rollback rehearsal, cleanup, remote link, or connection to production. Remote access was limited to CLI account visibility and a read-only Migration History query against the owner-approved Project.
- No CI, API, UI, role, route, workflow guard, checklist, Transition Executor, business schema, or business data change.
- Docker was neither installed nor made necessary.

## Rollback

Revoke the temporary CLI token, rotate the database password again, remove `.env.migration.local`, then remove the migration-safety scripts/tests and package scripts/dependency, restore package-lock.json and `.env.example`, remove Supabase init files, and revert README/.codex records. No schema or data rollback is required because no SQL or migration was executed.

## Task State

```yaml
CX-0190: BLOCKED
CX-0201: BLOCKED
```
