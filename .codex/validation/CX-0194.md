# Validation Record: CX-0194 — Gate A

**Task:** `CX-0194 — Adopt Canonical Migration Baseline`
**Phase:** `GATE_A`（Staging History Adoption = Gate B，未开始）
**Status:** `READY_FOR_STAGING_HISTORY_ADOPTION`
**Date:** 2026-08-07

## 摘要

Gate A 将 CX-0193 PROVEN 的候选 Baseline 冻结为仓库 Canonical Baseline
（`20260807120000_pg_os_canonical_baseline.sql`），把 24 条 Legacy Migration 归档为历史参考，
建立 `validate:migration-chain` 保护，并在 Migration Sandbox 上完成正式链两次空环境重建、
两次归一化 Diff（0 差异）、Migration History 校验与增量契约 Fixture 验证，最后清场恢复正式链状态。

## 冻结与 Manifest

```yaml
canonical_baseline:
  version: "20260807120000"
  adopted_from: CX-0193
  source_commit: "9be2500"
  candidate_sha256: a9f1fce5bc61c936b0c0933405b9d3222628f690b4756b16369b5eb9798a149d
  canonical_file_sha256: 59bfb9e7e01a6264b410c02d9614b577201a4cf1e3b79752f4bcf359428481eb
  semantic_hash: 59bfb9e7e01a6264b410c02d9614b577201a4cf1e3b79752f4bcf359428481eb
  manifest: supabase/baseline/manifest.yaml
  schema_counts: { tables: 178, columns: 3106, constraints: 528, indexes: 399, policies: 125, triggers: 51, functions: 15, views: 2, sequences: 4, extensions: 5 }
```

语义证明：Canonical 文件内容 = 候选 `00_extensions.sql` + `10_public_schema.sql` 逐字拼接，
字节哈希 = 语义哈希；`validate:migration-chain` 强制该等式与冻结哈希。

## Legacy 归档

```yaml
legacy_migration_archive:
  count: 24
  content_modified: false
  archived: true
  active_chain: false
  archive_dir: supabase/migrations-legacy/pre-canonical-baseline
  index: ARCHIVE.md（原路径/新路径/SHA-256）
  git_tracking: 24 个 100% rename
```

## Sandbox 正式链验证

```yaml
canonical_chain_rebuild_1:
  source: supabase/migrations
  record_history: true
  batches: 235
  result: SUCCESS
  history: [20260807120000]
  diff: 0 unexplained
canonical_chain_rebuild_2:
  result: SUCCESS
  batches: 235
  history: [20260807120000]
  diff: 0 unexplained
repeatability: PASS（同哈希 7aeb…? 正式链哈希见日志；同批次数、同 History、同 Diff）
sandbox_migration_history:
  expected_versions: [20260807120000]
  actual_versions: [20260807120000]
  unexpected_versions: []
  missing_versions: []
  result: PASS
```

注：正式链（`supabase/migrations` 单文件）重建批次为 235（候选双文件时为 236）；
正式链日志：`canonical-rebuild-1.json`、`canonical-rebuild-2.json`、`canonical-cleanup.json`。

## 增量 Migration 契约（Fixture，Sandbox-only）

```yaml
incremental_migration_contract:
  fixture_chain: supabase/.cx0194-fixture-chain（临时，已删除）
  fixture_version: 20260807130000_cx0194_increment_fixture.sql
  fixture_apply_1: SUCCESS（236 批，history=[20260807120000,20260807130000]，fixture 表存在）
  fixture_apply_2: SUCCESS（Reset 后重放一致）
  version_ordering: PASS
  apply_once: PASS
  history_recording: PASS
  duplicate_application_prevented: PASS（on conflict do nothing）
  rebuild_with_increment: PASS
  failure_detected: PASS（既有失败语义单测 + 历史写入失败单测）
  cleanup: 正式链 Reset 重建 SUCCESS（235 批），删除 fixture history 行，history=[20260807120000]
  final_diff: 0 unexplained
```

## 门禁

| 命令 | 结果 |
| --- | --- |
| `validate:baseline-environment` | PASS（Staging/Sandbox 写状态按阶段确认） |
| `validate:migration-chain` | PASS（1 个活动文件，Legacy 不在活动链） |
| `validate:schema-baseline` | PASS |
| `validate:cx0193` | PASS — 6 文件 / 59 测试 |
| `validate:cx0194` | PASS — 1 文件 / 6 测试 |
| `validate:workflow-machine` / `workflow-compatibility` / `domain-schema` / `phase2` | PASS |
| `lint` / `test` / `build` | PASS — 74 文件 / 435 测试；既有 bundle warning |
| `validate:phase18b` | PASS — 45.3s；secret hygiene 371 文本文件 |

## Staging History Adoption 分析（Gate B，未执行）

CLI 2.110.0 实测支持 `migration repair --status applied|reverted`、`migration list`、
`db push --dry-run/--include-all`。四方案比较见 `docs/adr/ADR-002`；
推荐 **OPTION_A**：保留 66 条 Legacy History，`migration repair --status applied 20260807120000`
标记 Baseline；未来 `db push` 只推新迁移。本轮零 Staging 写入。

## 状态

```yaml
CX-0194:
  status: READY_FOR_STAGING_HISTORY_ADOPTION
  gate_a: COMPLETED
  gate_b: NOT_STARTED
CX-0190: BLOCKED
CX-0201: BLOCKED
PG_OS_ENABLE_MIGRATION_WRITE: false
PG_OS_ENABLE_MIGRATION_SANDBOX_WRITE: false（人工恢复确认）
```

## 回滚

- 仓库：revert 采纳提交或恢复 `migrations-legacy` 路径；
- Sandbox：可 Reset；
- Staging：零写入，无需回滚；Gate B 未执行。
