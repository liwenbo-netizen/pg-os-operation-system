# CX-0201 Validation

**Result:** COMPLETED
**Date:** 2026-08-09
**Target:** repository, verified disposable Sandbox, and owner-attested non-production project

## Implemented Scope

- Additive `workflow_instances` and `workflow_transition_executions` persistence.
- Exact V1.8/V2.5 five-dimensional state and 29 stage-node invariant mappings.
- Positive `workflow_version`, unique idempotency key and immutable state snapshots.
- RLS enabled; direct `anon`/`authenticated` writes revoked; service path retained.
- Idempotent backfill only for the unambiguous `ECOSYSTEM_MAPPED` initial state.
- Compatibility view reports all other historical records as `LEGACY_REVIEW_REQUIRED`.
- Fail-closed rollback refuses to remove persistence after transition history exists.

## Database Evidence

- Repeat rebuild 1: 236/236 batches, zero failures.
- Repeat rebuild 2: 236/236 batches, zero failures, identical hash.
- Rollback: 1/1 batch; read-only proof found zero remaining additive objects.
- Finalized rebuild: 60/60 batches, zero failures; hash `3dd9cc9b745bc39627de2bee563c55d4ef92121b75da980cf8dbdaeab1e776c1` matches current migration files.
- Final Sandbox proof: tables/view/functions present, RLS enabled, browser inserts denied, non-mapped backfill count zero.
- Existing non-production remote preflight: only `20260809013000_workflow_persistence_foundation.sql` planned.
- Approved remote apply: exactly `20260809013000` applied; post-apply plan empty.
- Remote read-back: 467/467 mapped opportunities initialized, zero non-mapped rows backfilled, all additive objects present, RLS enabled, browser direct inserts denied.

## Validation

| Command | Result |
| --- | --- |
| `npm run validate:cx0201` | PASS; 5 files / 26 tests plus static and migration-chain checks |
| `npm run validate:cx0201:sandbox` | PASS; read-only database proof |
| `npm run db:cx0201:remote:dry-run` | PASS; one planned migration, zero writes |
| approved `npm run db:cx0201:remote -- --apply --approved-task=CX-0201` | PASS; exactly one migration applied; post-apply plan empty |
| `npm run validate:cx0201:remote` | PASS; remote read-only proof |
| `npm run lint` | PASS |
| `npm run build` | PASS; existing bundle-size warning only |
| `npm test` | PASS; 81 files / 468 tests |
| `npm run validate:phase18b` | PASS; 53.5s |

## Approval And Rollback

The repository owner explicitly approved this one expand-only migration and its `ECOSYSTEM_MAPPED`-only
backfill on the attested non-production project. No other migration, legacy field, or historical record was
modified or removed.

Rollback removes only CX-0201 additive objects while execution history is empty. Once history exists it
fails closed, preserving evidence and requiring the compatibility path instead.

## Next State

```yaml
CX-0201: COMPLETED
CX-0202: READY
```
